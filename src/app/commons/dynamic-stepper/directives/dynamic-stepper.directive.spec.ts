import { DynamicStepperDirective } from "./dynamic-stepper.directive";

describe("DynamicStepperDirective", () => {
  it("should create an instance", () => {
    const directive = new DynamicStepperDirective();
    expect(directive).toBeTruthy();
  });
});
